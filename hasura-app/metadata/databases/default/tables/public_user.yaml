table:
  name: user
  schema: public
array_relationships:
- name: organization_users
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: organization_user
        schema: public
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - name
    - id
    filter: {}
  role: global
