query getRequest($id: uuid!) {
  request_by_pk(id: $id) {
    id
    recipient_token_amount
    chain_id
    status
    recipient_token_address
    transaction_hash
    user_id
    owner {
      public_key
    }
  }
}
