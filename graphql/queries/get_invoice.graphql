query getInvoice($id: uuid!) {
  invoices_by_pk(id: $id) {
    id
    recipient_id
    recipient_token_address
    recipient_token_amount_raw
    chain_id
    status
    transaction_id
    recipient_user {
      address 
    }
  }
}
