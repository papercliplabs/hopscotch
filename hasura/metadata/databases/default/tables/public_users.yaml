table:
  name: users
  schema: public
array_relationships:
- name: organization_users
  using:
    foreign_key_constraint_on:
      column: user_id
      table:
        name: organization_users
        schema: public
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns:
    - client_last_requested
    - name
    - public_key
  role: anon
select_permissions:
- permission:
    columns:
    - name
    - id
    - public_key
    - nonce
    - created_at
    - updated_at
    - client_last_requested
    filter: {}
  role: anon
update_permissions:
- permission:
    check: null
    columns:
    - client_last_requested
    filter: {}
  role: anon
